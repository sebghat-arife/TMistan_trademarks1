# OPTIONAL Docker path (Render Static Site is the primary deployment).
# Only web/ (minus build output) and deploy/ enter the build context.
*
!web/
!deploy/
web/node_modules/
web/dist/
web/.env*
web/*.log
**/.env
**/.env.*
!**/.env.example
.git/
